# Claude Code

Install:

```bash
../../install.sh claude
```

Claude Code discovers these as `/micro-founder-loop`, `/micro-market-signal`, `/micro-offer-spec`, `/micro-build-loop`, and `/micro-launch-pack`.

Project-local install is also possible by copying or linking each directory under `skills/` into `.claude/skills/`.
