# Gemini CLI

Install as an extension:

```bash
gemini extensions link ../..
```

Or use the repository installer:

```bash
../../install.sh gemini
```

The extension manifest is `gemini-extension.json`; Gemini loads `GEMINI.md` as extension context and the Agent Skills from `skills/`.
