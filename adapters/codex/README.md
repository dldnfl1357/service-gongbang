# Codex

Install:

```bash
../../install.sh codex
```

Codex discovers skills from `${CODEX_HOME:-~/.codex}/skills/`. After installation, invoke one directly by name, for example:

```text
Use micro-founder-loop to turn this idea into a launch-ready V0 plan.
```
